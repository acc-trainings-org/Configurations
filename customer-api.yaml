# Service and Deployment configuration for Customer service deployment.

##################################################################################################
# Customer service
##################################################################################################
apiVersion: v1
kind: ConfigMap
metadata:
  name: customer-api-pod05
data:
  #spring.profiles.active: local
  spring.data.mongodb.uri: mongodb+srv://demo123:demo123@acctrainings.dntd4.mongodb.net/db-acc-trainings?retryWrites=true&w=majority
  spring.data.mongodb.database: db-acc-trainings
  my.path: /api/pod05
  springdoc.swagger-ui.operationsSorter: method
  springdoc.swagger-ui.path: /api/pod05/customer-api/swagger-ui.html
  #datastore.mongodb.collection.name: customer-coll-local
---
apiVersion: v1
kind: Service
metadata:
  name: customer-api-pod05
  labels:
    app: customer-api-pod05
    service: customer-api-pod05
spec:
  ports:
  - port: 8080
    name: http
  selector:
    app: customer-api-pod05
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-api-pod05
  labels:
    app: customer-api-pod05
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customer-api-pod05
      version: v1
  template:
    metadata:
      labels:
        app: customer-api-pod05
        version: v1
    spec:
      containers:
      - name: customer-api-pod05
        image: quay.io/acctrainings/customer-api-gold:main
        imagePullPolicy: Always
        envFrom:
          - configMapRef:
              name: customer-api-pod05
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Route
metadata:
  name: customer-api-pod05
  labels:
    app: customer-api-pod05
spec:
  to:
    kind: Service
    name: customer-api-pod05
