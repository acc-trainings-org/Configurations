apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: customer-api-virtual-service-pod05
spec:
  hosts:
  - '*.apps.awsopenshift.ne-innovation.com'
  gateways:
  - acctrainings-gateway-pod05
  http:
  - match:
    - uri:
        prefix: /api/pod05/customer
    route:
    - destination:
        host: customer-api-pod05
        port:
          number: 8080
