apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: policy-api-virtual-service-pod04
spec:
  hosts:
  - "*"
  gateways:
  - acctrainings-gateway-pod04
  http:
  - match:
    - uri:
        prefix: /api/pod04/policy
    route:
    - destination:
        host: policy-api-pod04
        port:
          number: 8080
    timeout: 5s
    retries:
      attempts: 3
      perTryTimeout: 2s
     
